<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.pms.mapper.IPmsAttrAttrgroupRelationMapper">
                <!-- 通用查询映射结果 -->
        <resultMap id="BaseResultMap" type="com.mall.pms.entity.PmsAttrAttrgroupRelation">
                                                                                                                            <result column="id" property="id"/>
                                                            <result column="attr_id" property="attrId"/>
                                                                <result column="attr_group_id" property="attrGroupId"/>
                                                                <result column="attr_sort" property="attrSort"/>
                                    </resultMap>

                <!-- 通用查询结果列 -->
        <sql id="Base_Column_List">
                            id,
                        attr_id, attr_group_id, attr_sort
        </sql>

        <select id="getAll" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM pms_attr_attrgroup_relation
    </select>
</mapper>